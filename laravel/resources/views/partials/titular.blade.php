<div class="d-flex flex-row flex-wrap justify-content-between align-items-baseline mb-3">
    <h1>{{ $titular }}</h1>
    @if(isset($subtitulo))
        <h2 class="text-secondary fw-light fs-6">{{ $subtitulo }}</h2>
    @endif
</div>
